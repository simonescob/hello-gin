version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - MY_VAR_EXAMPLE=example
      - APP_PORT=8080
    volumes:
      - .:/app
    restart: unless-stopped 